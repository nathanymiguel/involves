SELECT
  ID_PONTO_VENDA,
  ID_LINHA_PRODUTO,
  COUNT(*) AS QTD_PRESENCA
FROM
  `involves-422612.involves_datasets.involves-dataset`
WHERE
  EXTRACT(YEAR
  FROM
    DATA) = 2020
  AND EXTRACT(MONTH
  FROM
    DATA) = 9
  AND VALOR = 'SIM'
GROUP BY
  ID_PONTO_VENDA,
  ID_LINHA_PRODUTO
ORDER BY
  ID_LINHA_PRODUTO,
  ID_PONTO_VENDA,
  QTD_PRESENCA DESC;